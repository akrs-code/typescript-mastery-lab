/**
 * Entry point for the ESBuild + TypeScript integration example.
 * Injects basic HTML structure into the document body.
 */
document.body.innerHTML = `
    <h1>Learning ESBuild</h1>
    <p>Hello TypeScript</p>
`